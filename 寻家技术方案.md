## 寻家（Find Home）技术方案

### 核心架构

```
┌──────────────────────────────────────────┐
│  框架：Next.js 16 (App Router)            │
│  部署：Vercel（零配置，Git推送自动部署）     │
│  UI：shadcn/ui + Tailwind CSS v4         │
│  主题：next-themes（亮色/暗色/极简）       │
│  图标：Lucide React                       │
│  包管理：pnpm                             │
│  待接入：Supabase（数据库+认证）           │
│  待接入：Vercel AI SDK（AI分析）           │
└──────────────────────────────────────────┘
```

---

## 当前已实现（MVP前端）

| 功能 | 技术实现 | 状态 |
| --- | --- | --- |
| **房源表格** | shadcn/ui Table + 自定义列配置 | 已完成 |
| **表格内编辑** | 编辑模式切换，行内直接修改数据 | 已完成 |
| **自定义列** | 动态添加/删除列，支持文本/数字/日期类型 | 已完成 |
| **排序/搜索/筛选** | 表头排序 + 全局搜索 + 标签筛选 | 已完成 |
| **房源详情** | 右侧面板展示基础信息 + 看房时间轴 + AI分析 | 已完成 |
| **对比模式** | 勾选2-3套，右侧并排对比，差异高亮 | 已完成 |
| **收藏/状态** | 收藏标记 + 已看/待看/已售状态 | 已完成 |
| **主题切换** | 亮色/暗色/极简三种主题 | 已完成 |
| **悬浮添加** | 右下角浮动按钮快速添加房源 | 已完成 |

---

## 项目结构

```
findhome/
├── app/
│   ├── layout.tsx              # 根布局（主题、字体、Analytics）
│   ├── page.tsx                # 主页面（状态管理、组件组装）
│   └── globals.css             # 全局样式 + CSS变量
├── components/
│   ├── find-home/
│   │   ├── header.tsx          # 顶部导航（搜索、模式切换）
│   │   ├── property-table.tsx  # 房源大表格（核心组件）
│   │   ├── property-detail.tsx # 房源详情面板
│   │   ├── property-compare.tsx# 对比模式面板
│   │   └── floating-action-button.tsx # 悬浮添加按钮
│   ├── theme-provider.tsx      # 主题Provider
│   └── ui/                     # shadcn/ui 组件库
├── hooks/
│   ├── use-mobile.ts           # 移动端检测
│   └── use-toast.ts            # Toast通知
├── lib/
│   ├── mock-data.ts            # Mock房源数据（6套示例）
│   └── utils.ts                # 工具函数
├── types/
│   └── property.ts             # 类型定义（Property、ViewMode、ColumnConfig等）
└── styles/
    └── globals.css             # 额外样式
```

---

## 开发路线图

### 已完成：前端MVP

- 房源大表格（列表/编辑/对比三种模式）
- 房源详情面板（基础信息 + 看房记录时间轴 + AI分析占位）
- 排序、搜索、标签筛选
- 自定义列、收藏、状态管理
- Mock数据展示

### 下一步：数据持久化

- Supabase 创建数据库表（properties、viewing_records）
- 替换 Mock 数据为真实 CRUD
- Supabase Auth 接入（邮箱+密码登录）
- Row Level Security 数据隔离

### 后续：AI + 增强功能

- Vercel AI SDK 接入 OpenAI，替换 Mock AI 分析数据
- 多房源对比智能报告
- 图片上传（Supabase Storage）
- 导出 Excel/PDF
- 移动端适配优化

---

## 本地开发

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建
pnpm build
```

---

## 部署

```bash
# 代码推送到GitHub，Vercel自动构建部署
git push origin main

# 域名：https://findhome-xxx.vercel.app
```

v0.app 项目链接：可通过 v0 继续迭代前端界面，每次合并到 main 自动部署。

---

## 成本估算

| 服务 | 免费额度 | 超出后费用 |
| --- | --- | --- |
| **Vercel** | 100GB带宽/月 | Pro版$20/月 |
| **Supabase**（待接入） | 500MB数据库，1GB存储，50,000 MAU | $25/月起 |
| **OpenAI API**（待接入） | 无免费额度 | 按Token计费 |

**当前阶段成本**：0元（纯前端，无后端服务）
